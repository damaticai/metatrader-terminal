name: Remote SSH Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "Remote SSH deploy is disabled. GHCR publishing is handled by publish-ghcr.yml."
