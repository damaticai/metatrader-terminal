# WebSocket upgrade mapping
# Include this at the http {} level in your main nginx.conf:
#   include /etc/nginx/snippets/websocket_map.conf;
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
